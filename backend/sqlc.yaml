version: "2"

sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: "query"
    gen:
      go:
        package: "db"
        out: "internal/infrastructure/db/sqlc"
        sql_package: "database/sql"
        emit_json_tags: true
